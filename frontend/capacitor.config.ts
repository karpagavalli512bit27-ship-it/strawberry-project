import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.strawberry.detector',
  appName: 'Strawberry Detector',
  webDir: 'build'
};

export default config;
